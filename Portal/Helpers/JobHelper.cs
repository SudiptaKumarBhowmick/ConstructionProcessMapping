using Portal.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Portal.Helpers
{
    public class JobHelper
    {
        //public static List<Job> JobFieldGenerator(List<Job> job)
        //{
        //    List<Job> rawJobList = new List<Job>();
        //    for (int i = 0; i < job.Count(); i++)
        //    {
        //        if (job[i].MasterName == "Custom Input")
        //        {
        //            List<String> customInputs = new List<string>();
        //            customInputs.Add(job[i].DisplayedText);
        //            job[i].CustomInputCount = customInputs.Count();
        //        }
        //        if (job[i].MasterName == "Custom Output")
        //        {
        //            List<String> customOutputs = new List<string>();
        //            customOutputs.Add(job[i].DisplayedText);
        //            job[i].CustomOutputCount = customOutputs.Count();
        //        }
        //    }
        //}
    }
}
